apiVersion: v1
kind: Service
metadata:
  name: assistentes-gpt-frontend-service
  namespace: assistentes-gpt
spec:
  type: NodePort
  selector:
    app: assistentes-gpt-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
